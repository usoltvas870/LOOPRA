LOOPRA 0.6 — вход для Comment Intelligence

Создайте рядом файл selected_video.txt в UTF-8 или UTF-8 BOM.
Укажите в нём ровно одну публичную ссылку TikTok на видео.
Пустые строки и строки, начинающиеся с #, игнорируются.

Не добавляйте реальную ссылку в Git.

Запуск:
python scripts/run_loopra_06_comment_intelligence.py --project nura
